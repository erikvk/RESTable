namespace RESTable.Client
{
    /// <summary>
    /// Similarly to RESTableContext, defines a context in which the interactions with
    /// RESTable APIs can take place, but for an external service rather than a local.
    /// </summary>
    public class RemoteRESTableContext
    {
        
    }
}